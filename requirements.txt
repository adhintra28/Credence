# Core (MVP must-have)
pandas>=2.0
numpy>=1.24
pyarrow>=12.0
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
shap>=0.44
mlflow>=2.9
plotly>=5.18
dash>=2.14
flask>=3.0
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pyyaml>=6.0
pytest>=7.4
python-dateutil>=2.8

# Optional heavy stack — install as needed (guarded imports, MVP runs without these)
# Feast (feature store)
# feast[redis]>=0.35
# Stream processing
# kafka-python>=2.0
# Orchestration
# apache-airflow>=2.8
# Model serving
# bentoml>=1.2
# Deep learning (sequence models)
# torch>=2.1
# tensorflow>=2.15
# AWS stack (all commented code until you configure backend)
# boto3>=1.34
# sagemaker>=2.200
